import React, { memo } from 'react';
import { SelectComponent } from '../app_components';

const MainPage = memo(() => {
   return (<>
      <div>MainPage</div>
      <SelectComponent />
   </>)
});

export default MainPage;